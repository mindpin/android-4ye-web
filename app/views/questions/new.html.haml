.page-question-form.new
  %h1 新建测试题
  .nav
    = link_to "首页", questions_path
    >
    = link_to @net.name, net_questions_path(:id => "javascript")
    >
    = link_to @node.set.name, set_questions_path(:id => @node.set.id, :net_id => @net.id)
    >
    = link_to @node.name, node_questions_path(:id => @node.id, :net_id => @net.id)
    >
    = link_to "新建测试题", new_question_path(:id => @node.id, :net_id => @net.id), :class => "current"
  .question-form
    = render "form"
