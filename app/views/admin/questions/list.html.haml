.page-questions
  .header
    %h1 测试题列表
  .question-list
    - @questions.each do |question|
      .question
        .content= question.content
        .ops
          = link_to "编辑", edit_admin_question_path(question)
          = link_to "删除", admin_question_path(question), :method => :delete
